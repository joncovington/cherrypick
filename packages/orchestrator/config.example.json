{
  "identity_header": "x",
  "_comment": "cherrypick orchestration config. Copy to config.json and adjust. A module is located by 'path' (relative to this file's directory \u2014 in the monorepo every module ships in the same checkout) or, for an out-of-tree module, a 'repo' URL cloned by `cherrypick install` into ~/.cherrypick/modules/<repo-name>. Never hardcode absolute machine paths.",
  "_home_note": "Runtime home (this config.json, logs/, state/, dashboard.html) = CHERRYPICK_HOME if set, else the repo root in a source checkout, else ~/.cherrypick for a pip-installed copy. An installed copy thus reads ~/.cherrypick/config.json.",
  "_modules_home_note": "Managed module checkouts live under CHERRYPICK_MODULES_HOME, else CHERRYPICK_HOME/modules, else ~/.cherrypick/modules. `install` clones a module only when its checkout is absent and never touches an existing one; `uninstall` leaves checkouts in place.",
  "timezone": "America/New_York",
  "modules": {
    "meic": {
      "enabled": true,
      "_path_note": "Locate the module by 'path' (absolute or relative to this file). In the monorepo every module ships in the same checkout, so the relative path is the way; a 'repo' clone URL is still honored for an out-of-tree module, but the pre-monorepo split-repo URLs were removed 2026-07-29 so a path miss can never clone retired code.",
      "path": "../meic",
      "_keyring_note": "The module's OS-keyring service name, used by `cherrypick account`/`connect` to read/select the module's live-trading account (ACCOUNT_NUMBER) via the shared CredentialStore. Optional 'keyring_legacy_services' lists read-only fallbacks (MEIC keeps 'tastytrade-mcp' for pre-rename creds). Omit to disable account selection for this module.",
      "keyring_service": "meicagent",
      "keyring_legacy_services": [
        "tastytrade-mcp"
      ],
      "_live_db_note": "Phase-5 seam: the module's LIVE-trades ledger, read ONLY by the explicitly live-tagged surfaces (`cherrypick report --live`). Deliberately a separate key from paper.paper_db \u2014 calibrate and every promotion reading go through the paper path and can never see this file. Same resolution rules as paper_db (absolute / ~-expanded / module-root relative).",
      "live_db": "~/.cherrypick/data/meic/meic_trades.db",
      "paper": {
        "kind": "self_healing",
        "task_name": "cherrypick-meic-paper-loop",
        "_tick_interval_note": "Supervisor cadence for the paper tick (spawns once_argv sans --force every N seconds). 60 halves the old hardcoded 2-minute schtasks cadence (2026-08-09 supervisor cutover) for tighter exit management on open condors. task_name/install_argv/uninstall_argv are the legacy schtasks path -- still used by the module standalone, retained here so install/uninstall can delete the old task by name.",
        "tick_interval_seconds": 60,
        "install_argv": [
          "-m",
          "cherrypick.meic.paper_loop",
          "--install-task"
        ],
        "uninstall_argv": [
          "-m",
          "cherrypick.meic.paper_loop",
          "--uninstall-task"
        ],
        "status_argv": [
          "-m",
          "cherrypick.meic.paper_loop",
          "--status"
        ],
        "once_argv": [
          "-m",
          "cherrypick.meic.paper_loop",
          "--once",
          "--force"
        ],
        "_paper_db_note": "Path to the module's paper-trades SQLite DB \u2014 MUST point at the file the module actually WRITES (a mismatch silently blinds the orchestrator's report/reconcile/dashboard and the watchdog freshness check). Relative resolves against the module checkout; a leading '~' or env var expands (use e.g. '~/.cherrypick/data/meic/paper_trades.db' when the module keeps its data in the managed home); an absolute path is used as-is.",
        "paper_db": "data/paper_trades.db",
        "notify_trades": true,
        "trade_schema": "meic_ic",
        "log": "paper_loop.log",
        "freshness_minutes": 20
      },
      "streamer": {
        "_note": "MEIC's own in-module streamer \u2014 DISABLED since the 2026-07-21 producer cutover: the standalone streamer (top-level `streamer` block below) is the suite's sole producer, and exactly ONE producer ever runs. Kept only as the rollback path: to revert, set enabled=true here AND streamer.enabled=false below.",
        "enabled": false,
        "status_argv": [
          "-m",
          "cherrypick.meic.streamer",
          "--status"
        ],
        "start_argv": [
          "-m",
          "cherrypick.meic.streamer"
        ],
        "_stop_argv_note": "Optional. Used to stop a STALLED streamer before relaunching it \u2014 the daemon is single-instance guarded, so relaunching without stopping is a silent no-op. Defaults to the status command with --stop.",
        "stop_argv": [
          "-m",
          "cherrypick.meic.streamer",
          "--stop"
        ],
        "auto_restart": true,
        "_stale_restart_note": "Restart the streamer when it is CONNECTED but has received no market events for this many seconds during RTH. A process check cannot see this: observed 2026-07-20, the streamer reconnected then went silent for 8 minutes while still reporting running=true and its own stale_warning=false (that flag only trips at 600s), so nothing restarted it \u2014 MEIC degraded to REST and the flies module refused every iteration on stale quotes. 240s is well inside the 600s self-report and well outside a normal quiet tick. Restarts are skipped while a connection is younger than this, so a just-restarted streamer is not judged before it has resubscribed.",
        "stale_restart_seconds": 240
      },
      "calibration": {
        "_comment": "Optional. `cherrypick calibrate` reads paper P&L per profile and advises whether a qualified challenger should replace the live 'champion' (advisory only; never switches live risk). 'champion' names the currently-live profile -- every other tag observed is a challenger compared against it on return_on_capital (falling back to net_pnl). Omit 'champion' entirely to get per-tag qualification readings with NO champion/challenger comparison (for a module whose tags are parallel experiments rather than a risk sequence -- see flies below). 'deliberate_only' tags are never auto-recommended even if they qualify and beat the champion; 'rule' overrides the default {min_days:14, min_win_rate:0.6, min_sample:20} and may add 'margin' (default 0.0 -- any positive edge counts as beating the champion).",
        "_arms_cutover_note": "Updated 2026-08-07 for the arms cutover (config.risk.json's _arms_cutover_note): champion is now `control` (today's deployed policy, formerly `conservative` -- same values, new name). The retired ladder/GEX tags (conservative/moderate/aggressive/very-aggressive/gex-open/gex-blocked) never write a new row again, so they simply stop appearing as challengers going forward; their historical rows stay under their own tag and never blend into `control`'s reading, since the tag NAME itself is what segregates the eras here -- no data_epoch needed (that key is suite-wide and would also truncate flies/earnings' unrelated history). `open` is deliberate_only: it runs with per_side_stop_management OFF (held to settlement/force-close, no per-side stop at all), a materially different risk posture from control's canonical MEIC+ stop, not just a quality-gate relaxation -- qualifying on paper should not auto-promote a no-stop policy without a human decision, the same reasoning very-aggressive's deliberate_only carried.",
        "champion": "control",
        "deliberate_only": [
          "open",
          "very-aggressive"
        ],
        "rule": {
          "min_days": 14,
          "min_win_rate": 0.6,
          "min_sample": 20
        }
      }
    },
    "flies": {
      "_comment": "cherrypick-flies: 0DTE net-credit butterflies ('profit forest'), paper only, XSP (SPX through 2026-07-28). Reads the shared stream cache READ-ONLY and runs no streamer of its own, so it REQUIRES the suite's streamer to be running and subscribed to its symbol. ENABLED, but it has still never completed a live session \u2014 watch the first one, in particular the quote_stats line in the log (which separates 'the streamer gave us nothing' from 'the strategy found nothing') and the arm_divergence figure (if the arms agree nearly always, the comparison cannot answer anything and max_center_distance_pct is the first thing to revisit).",
      "enabled": true,
      "path": "../flies",
      "_broker_note": "Live-scaffold onboarding (docs/live-trading-plan.md in the module): flies has no tt.py, so its config-declared broker/credential CLI is broker_cli.py; `cherrypick connect/account --module flies` drive it exactly like MEIC's tt.py. keyring service fliesagent; live_db is the SEPARATE live ledger read only by `report --live` (never by calibrate/promotion).",
      "keyring_service": "fliesagent",
      "broker_tool": [
        "-m",
        "cherrypick.flies.broker_cli"
      ],
      "live_db": "~/.cherrypick/data/flies/live_trades.db",
      "_live_note": "Watchdog + notification wiring for the module's LIVE loop (the module itself owns arming/trading; this block only tells the orchestrator how to WATCH it). task_name is the module's self-registered live task; the loop self-disarms at the module's live.disarm_time (arming is per-day) and disarm_time/disarm_grace_minutes here are the watchdog's BACKSTOP window \u2014 past it, a still-registered live task means self-disarm failed, so the watchdog sets the suite halt flag (risk-reducing only; it never touches the task) and raises CRITICAL. notify_trades pushes live entries/completions/settlements from live_db with a hard-to-miss LIVE prefix + desktop toast, from the notifier path (never the trading loop).",
      "live": {
        "task_name": "cherrypick-flies-live-loop",
        "status_argv": [
          "-m",
          "cherrypick.flies.live_loop",
          "--status"
        ],
        "log": "flies_live.log",
        "freshness_minutes": 5,
        "notify_trades": true,
        "disarm_time": "17:00",
        "disarm_grace_minutes": 30,
        "settlement_grace_minutes": 30
      },
      "paper": {
        "_kind_note": "self_healing, NOT cherrypick_scheduled. This was cherrypick_scheduled, which registers one entry task at entry_time and one exit task at exit_time \u2014 a shape that fits Earnings (one entry, one exit per day) and is wrong for a legged 0DTE strategy. A single daily entry would open a credit spread at noon and never look at it again, so the completing spread could never be bought and the completion rate \u2014 the module's headline measurement \u2014 would read 0% every day for reasons that have nothing to do with the market. cherrypick_scheduled also hardcodes the earnings launcher verbs, so this module's own entry_argv would have been ignored entirely.",
        "kind": "self_healing",
        "task_name": "cherrypick-flies-paper-loop",
        "_timing_note": "tick_interval_seconds is the supervisor cadence. Sub-minute (<60) runs the module's own resident `--interval N` loop IN-SESSION (09:30-16:00 ET, supervised: restarted on death and on silence_seconds of log silence) plus a 60s `--once` spawn job OUTSIDE the session, so settlement at 16:20, retry-until-settled, and the hourly idle heartbeat keep exactly the shape the 1-minute task gave them. 15s (2026-08-09 supervisor cutover, was 60 -- the OS scheduler's floor) samples transient completion-debit dips ~4x finer; the completion rate is NOT comparable across that date (any discrete poll understates what a resting limit order would catch, so faster polling measures a truer, higher rate). The change is journaled in the module's decision journal and its CLAUDE.md. task_name/install_argv/uninstall_argv are the legacy schtasks path, retained for deletion by name.",
        "tick_interval_seconds": 15,
        "silence_seconds": 120,
        "install_argv": [
          "-m",
          "cherrypick.flies.paper_loop",
          "--install-task"
        ],
        "uninstall_argv": [
          "-m",
          "cherrypick.flies.paper_loop",
          "--uninstall-task"
        ],
        "status_argv": [
          "-m",
          "cherrypick.flies.paper_loop",
          "--status"
        ],
        "_once_note": "No --force. --force bypasses the RTH gate AND the trading-day gate, the second of which exists to stop a Saturday tick from 'settling' a session that never happened and emitting a paper-eod-<saturday>.md the suite digest would ingest as real. The module's own --install-task registers plain `--once`, so --force here would also mean the orchestrator drives the loop differently from its own scheduled task.",
        "once_argv": [
          "-m",
          "cherrypick.flies.paper_loop",
          "--once"
        ],
        "paper_db": "~/.cherrypick/data/flies/paper_trades.db",
        "notify_trades": true,
        "trade_schema": "fly_book",
        "log": "flies_paper.log",
        "freshness_minutes": 20,
        "_settlement_check_note": "Opt in the watchdog's settlement-overdue alert: past the settle time + retry buffer on a trading day it reads this module's --status and WARNs if session_settled is false with positions_today > 0 (a settle stuck on a stale feed, which the freshness check misses because the loop is still running). Only set for a module whose --status reports session_settled/positions_today (flies does). settlement_grace_minutes is minutes AFTER the 16:00 close before 'overdue' applies -- it must cover the module's own settle time (flies settles at 16:20) plus a few retries, or it false-alarms every day in the 16:00-settle window. Default 30 (fires at 16:30).",
        "settlement_check": true,
        "settlement_grace_minutes": 30
      },
      "calibration": {
        "_comment": "These tags are parallel experiment arms (control/gex/time_window/width-N), not a risk sequence -- there is no currently-live 'champion' among them, so no 'champion' key is set: cherrypick calibrate produces per-arm qualification readings with NO champion/challenger recommendation. width-2..width-5 are the wing-width sweep (control at the default width is its 1-increment rung); wide_wing is the retired SPX-era single-point width arm, listed so its historical books stay readable.",
        "rule": {
          "min_days": 14,
          "min_win_rate": 0.6,
          "min_sample": 20
        }
      }
    },
    "earnings": {
      "enabled": true,
      "path": "../earnings",
      "keyring_service": "earningsagent",
      "paper": {
        "kind": "cherrypick_scheduled",
        "entry_task_name": "cherrypick-earnings-paper-entry",
        "exit_task_name": "cherrypick-earnings-paper-exit",
        "entry_time": "15:45",
        "exit_time": "09:45",
        "entry_argv": [
          "-m",
          "cherrypick.earnings.strategy_test_runner",
          "run_entries",
          "--date",
          "{today}"
        ],
        "exit_argv": [
          "-m",
          "cherrypick.earnings.strategy_test_runner",
          "run_closes"
        ],
        "log": "earnings_paper.log",
        "paper_db": "~/.cherrypick/data/earnings/paper_trades.db",
        "notify_trades": true,
        "trade_schema": "earnings",
        "requires_dolt": true,
        "dolt_host": "127.0.0.1",
        "dolt_port": 3306,
        "dolt_user": "root",
        "_dolt_databases_note": "Databases the runner needs served on dolt_host:dolt_port. doctor verifies each is present (not just that the port answers) when a MySQL client is available, catching a Dolt server rooted at the wrong data dir. Omit to check port reachability only.",
        "dolt_databases": [
          "earnings",
          "options",
          "stocks"
        ],
        "_dolt_service_note": "Optional cherrypick-managed keep-alive. `install` registers a MINUTE task that runs `ensure-dolt` (starts `dolt sql-server` from data_dir only when nothing is on the port); `uninstall` removes it; `status`/`doctor` surface it. data_dir is portable: `~` expands, a relative path resolves against the cherrypick runtime ROOT. Omit this block to manage Dolt yourself.",
        "dolt_service": {
          "task_name": "cherrypick-earnings-dolt",
          "data_dir": "~/.cherrypick/data/earnings",
          "interval_minutes": 5
        }
      },
      "_calibration_note": "Earnings retired its conservative/balanced/aggressive risk-profile ladder; the strat_test paper book is now split per strategy (profile tag strat_test:<strategy>). With an empty ladder, calibrate reports per-book readings only (no promotion recommendation) -- strategies don't graduate rungs the way a risk ladder does.",
      "calibration": {}
    }
  },
  "watchdog_header": "x",
  "watchdog": {
    "task_name": "cherrypick-watchdog",
    "interval_minutes": 10,
    "renotify_minutes": 60,
    "_streamer_health_note": "The dedicated STREAMER-ONLY liveness job: every interval_seconds across the whole session (start-end, ET, trading days), as its own supervisor job so the full 10-minute tick above never has to speed up to protect the streamer. Successor to the retired cherrypick-preopen windowed task (whose reasons all survive: a producer dead overnight must be caught before the unrecoverable 09:30-09:35 opening range, and a restart still needs the 240s settling window) with strictly broader coverage -- silence is now caught within ~5 min all session, not just pre-open. ON by default for the same reason preopen was.",
    "streamer_health": {
      "enabled": true,
      "interval_seconds": 60,
      "start": "09:00",
      "end": "16:00"
    },
    "_preopen_note": "RETIRED (2026-08-09, supervisor cutover) -- replaced by streamer_health above. The block remains only so install/uninstall can delete the legacy cherrypick-preopen task by its resolved name; remove it once every machine has cut over.",
    "preopen": {
      "enabled": true,
      "task_name": "cherrypick-preopen",
      "interval_minutes": 2,
      "start": "09:00",
      "end": "09:35"
    },
    "_drawdown_note": "Optional paper-P&L drift alert (net-of-cost). Add a 'drawdown' block to enable: {\"suite_floor\": -1000, \"module_floors\": {\"meic\": -750, \"earnings\": -750}, \"critical_multiplier\": 2}. net<=floor -> WARN; net<=floor*critical_multiplier -> CRITICAL. Paper only; never trades. Omit to disable."
  },
  "_eval_activity_note": "Eval-activity health for the continuous-loop modules (meic/flies): during RTH the watchdog + doctor read the module's eval log (MEIC loop_log, flies fly_snapshots) and confirm it is actually EVALUATING candidates and deciding sensibly, not just writing a file (freshness). Rejecting every candidate is HEALTHY (a legit gate, e.g. regime_gex_negative), so it WARNs only when: no evaluation for stale_minutes (stopped mid-session), iterating but evaluating nothing, >= error_fraction of evaluations erroring (e.g. a symbol with no price), or 0 entries for a reason that isn't a known gate. window_minutes is the lookback. Read-only; earnings is event-driven and covered by its entry-SLA check instead.",
  "eval_activity": {
    "window_minutes": 30,
    "stale_minutes": 10,
    "error_fraction": 0.5
  },
  "dashboard_header": "x",
  "dashboard": {
    "_comment": "Read-only status dashboard: static self-contained HTML regenerated on each watchdog tick and by `cherrypick dashboard`. Surfaces suite health + P&L + logs, plus a System panel (scheduled tasks, installed modules, and a redacted config summary \u2014 webhook state only, never URLs). Under --serve it adds a live checks card that polls doctor.run() (the only broker-touching part, kept off the static path). 'output' is relative to this file's directory.",
    "output": "dashboard.html",
    "log_tail_lines": 50,
    "auto_regen": true,
    "serve": {
      "_comment": "`cherrypick dashboard --serve` runs a localhost live view (same page rebuilt per request) instead of writing the static file. Loopback-only, read-only. Override host/port on the CLI with --host/--port.",
      "host": "127.0.0.1",
      "port": 8787
    },
    "sections": [
      {
        "_comment": "Optional live cards for `dashboard --serve`. Each section is subprocessed for a cherrypick.core.viz payload and rendered generically (the orchestrator never imports the module). A new module gets a card by emitting the schema \u2014 add it here. Locate the module by 'path' (in-place checkout, relative to this file) or 'repo'. fetch_argv may use {symbol} etc. Deliberately NOT under 'modules' (that registry is for paper pipelines).",
        "id": "gex",
        "title": "GEX",
        "enabled": false,
        "path": "../gex",
        "fetch_argv": [
          "run.py",
          "section",
          "--symbol",
          "{symbol}",
          "--json"
        ],
        "default_symbol": "SPX",
        "refresh_seconds": 15
      },
      {
        "_comment": "cherrypick-flies' payoff curve. The viz `bars` contract (signed values, tone_by_sign, a focus marker) was built for net-GEX-by-strike but maps exactly onto payoff-by-price, so this card draws the 'profit forest' itself \u2014 green across the band where the book profits, red outside it, focus at spot \u2014 with no flies-specific rendering code here.",
        "id": "flies",
        "title": "Flies",
        "enabled": true,
        "path": "../flies",
        "fetch_argv": [
          "run.py",
          "section",
          "--json"
        ],
        "refresh_seconds": 30
      },
      {
        "_comment": "cherrypick-meic's iron-condor card: net-of-fees P&L tiles + the cumulative daily P&L curve as a date-axis timeseries, read through the module dashboard's own query helpers. Paper mode is forced in the argv, same rule as the embeds.",
        "id": "meic",
        "title": "MEIC",
        "enabled": false,
        "path": "../meic",
        "fetch_argv": [
          "-m",
          "cherrypick.meic.section",
          "--mode",
          "paper",
          "--json"
        ],
        "refresh_seconds": 30
      },
      {
        "_comment": "cherrypick-earnings' strategy-test card: KPI tiles (net P&L, expectancy, sample progress) + the portfolio equity curve as a date-axis timeseries. Paper mode is forced in the argv, same rule as the embeds. The book only moves at the daily entry/exit passes, so a slow refresh is plenty.",
        "id": "earnings",
        "title": "Earnings",
        "enabled": false,
        "path": "../earnings",
        "fetch_argv": [
          "-m",
          "cherrypick.earnings.strategy_section",
          "--mode",
          "paper",
          "--json"
        ],
        "refresh_seconds": 60
      }
    ],
    "embeds": [
      {
        "_comment": "Optional embedded module dashboards for `dashboard --serve` only: each module's OWN full dashboard, shown in an iframe on the orchestrator page. The orchestrator drives the module by config-declared argv (never imports it) and PAPER mode must be forced in that argv. Locate the module by 'path' (relative to this file) or 'repo'. Two kinds: 'server' (module runs its own HTTP dashboard \u2014 orchestrator launches it if the port is down, iframe points at {port}) and 'static' (module regenerates a self-contained HTML file \u2014 orchestrator runs build_argv, throttled to refresh_seconds, and serves 'output'). Disabled by default.",
        "id": "meic",
        "title": "MEIC",
        "enabled": false,
        "kind": "server",
        "path": "../meic",
        "serve_argv": [
          "-m",
          "cherrypick.meic.dashboard",
          "--mode",
          "paper",
          "--port",
          "{port}"
        ],
        "port": 8801
      },
      {
        "id": "earnings",
        "title": "Earnings",
        "enabled": false,
        "kind": "static",
        "path": "../earnings",
        "build_argv": [
          "-m",
          "cherrypick.earnings.strategy_dashboard",
          "--mode",
          "paper"
        ],
        "output": "~/.cherrypick/data/earnings/reports/strategy_dashboard.html",
        "refresh_seconds": 30
      },
      {
        "_comment": "The gex module's full GEX/IV-Skew/Volume dashboard (read-only off the shared stream cache). Complements the compact live GEX section card above.",
        "id": "gex",
        "title": "GEX Dashboard",
        "enabled": false,
        "kind": "server",
        "path": "../gex",
        "serve_argv": [
          "run.py",
          "dashboard",
          "--serve",
          "--port",
          "{port}"
        ],
        "port": 8802
      },
      {
        "_comment": "cherrypick-flies' full dashboard: the live payoff curve, open positions with their post-fee floors, the decision journal (why each entry was made or refused), and P&L / win rate / completion rate over timeframes. Served with --paper-only: this card is badged PAPER, and the module's dashboard otherwise offers a live -- real money source selector, which would let a viewer switch the embedded card to the live ledger while the badge still read PAPER. --paper-only strips that control AND makes the server refuse source=live. Loopback-only, read-only, no broker and no network.",
        "id": "flies",
        "title": "Flies",
        "enabled": true,
        "kind": "server",
        "path": "../flies",
        "serve_argv": [
          "-m",
          "cherrypick.flies.dashboard",
          "--port",
          "{port}",
          "--paper-only"
        ],
        "port": 8803
      }
    ]
  },
  "settings_header": "x",
  "settings": {
    "_comment": "`cherrypick settings` \u2014 the local web editor for the suite's config files and keyring secrets. The suite's ONE mutating HTTP surface: loopback-only + per-session-token gated, run on demand in the foreground, never started by the watchdog or a scheduled task, and live-trading gate fields are read-only on it. Port 8804 is the next slot after the 88xx embeds (8801 meic, 8802 gex, 8803 flies).",
    "serve": {
      "host": "127.0.0.1",
      "port": 8804
    }
  },
  "streamer_header": "x",
  "streamer": {
    "_comment": "The suite's standalone market-data producer (packages/streamer) \u2014 the single streamer that writes the shared cache ~/.cherrypick/data/marketdata/stream_cache.db, which every module reads. THE sole producer since the 2026-07-21 cutover (modules.meic.streamer is the disabled rollback path; exactly ONE producer runs at a time). Session-gated and silence-restarted by the watchdog. Located by 'path' (relative to this file). Modules declare which symbols they need via ~/.cherrypick/state/stream_requests/<module>.json \u2014 every module regenerates its own file on each run \u2014 and the streamer streams the union.",
    "enabled": true,
    "path": "../streamer",
    "status_argv": [
      "run.py",
      "--status"
    ],
    "start_argv": [
      "run.py"
    ],
    "stop_argv": [
      "run.py",
      "--stop"
    ],
    "auto_restart": true,
    "stale_restart_seconds": 240
  },
  "services": [
    {
      "_comment": "Long-running background daemons the orchestrator keeps alive: started at `install`, restarted by the watchdog if down (single-instance guarded), stopped at `uninstall`. Located by 'path' (relative to this file) or 'repo'. status_argv must print {\"running\": bool}. A running service whose config changed since launch is recycled (stop then start) so it re-reads -- the orchestrator stamps a hash of this entry plus the service's own config file at launch and compares each tick; requires auto_restart, and set 'config_name' if that config file is named for neither the checkout directory nor the id. The gex spot-trail recorder samples every symbol's spot continuously so the GEX dashboard's spot trail has no gaps and builds all session, independent of whether the dashboard is open. Disabled by default.",
      "id": "gex-recorder",
      "enabled": false,
      "path": "../gex",
      "status_argv": [
        "run.py",
        "record",
        "--status"
      ],
      "start_argv": [
        "run.py",
        "record"
      ],
      "stop_argv": [
        "run.py",
        "record",
        "--stop"
      ],
      "auto_restart": true
    },
    {
      "_comment": "MEIC's post-cutover sidecar: the REST account poller + the 127.0.0.1:7699 HTTP API ONLY, no DXLink streaming (the standalone `streamer` above is the market-data producer). It serves the shared cache read-only and caches account/positions/market-overview into MEIC's own rest_cache.db, so MEIC's interactive loop keeps its REST/quote fast-path without a second writer on the shared cache. Optional and OFF by default \u2014 MEIC works without it (quotes read the cache directly; account calls just go live). Enable it if you run MEIC's live/interactive loop and want the cached fast-path back.",
      "id": "meic-sidecar",
      "enabled": false,
      "path": "../meic",
      "status_argv": [
        "-m",
        "cherrypick.meic.streamer",
        "--sidecar",
        "--status"
      ],
      "start_argv": [
        "-m",
        "cherrypick.meic.streamer",
        "--sidecar"
      ],
      "stop_argv": [
        "-m",
        "cherrypick.meic.streamer",
        "--sidecar",
        "--stop"
      ],
      "auto_restart": true
    }
  ],
  "notify_header": "x",
  "trade_notify": {
    "_comment": "Dedicated low-latency supervisor job that pushes new paper entries/exits to trade_channels. Runs far more often than the watchdog so fills reach you within ~30s instead of a full watchdog cycle. interval_seconds is the supervisor cadence (sub-minute became possible when the OS scheduler's 1-minute floor went away, 2026-08-09); a legacy interval_minutes is still honored if set explicitly. task_name is retained only so install/uninstall can delete the pre-supervisor task by name.",
    "task_name": "cherrypick-trade-notify",
    "interval_seconds": 30
  },
  "follow_feed": {
    "_comment": "Push tastylive Follow Feed orders (other traders' fills, as shown on the platform's Follow page) to their own Discord channel. Reads two undocumented public GETs on https://follow.tastylive.com - no auth, no broker, read-only. UNLIKE every other notifier this one makes a NETWORK call, so it is its own scheduled task and is never called from the watchdog tick (the reliability path stays stdlib + OS shell only). A feed outage degrades to 'no notifications', never a failed tick. OFF by default; set enabled=true, store the webhook (cherrypick secrets-set --channel discord_follow), then re-run `install`.",
    "_filters_note": "All filters are optional and applied server-side by the feed itself; omit or leave empty for the whole feed. 'traders' are display names as they appear in /api/traders ('Jim Schultz', 'Katie'); 'underlying_symbols' an equity symbol list; 'earnings_only' true limits to earnings plays; 'open_close' is 'O' (opening orders only) or 'C' (closing only); 'strategy' matches one strategy name ('Vertical', 'Butterfly', 'Strangle').",
    "enabled": false,
    "task_name": "cherrypick-follow-notify",
    "interval_minutes": 5,
    "channels": [
      "log",
      "discord_follow"
    ],
    "max_per_run": 8,
    "filters": {
      "traders": [],
      "underlying_symbols": [],
      "earnings_only": false,
      "open_close": null,
      "strategy": null
    }
  },
  "notify": {
    "_channels_note": "push channels: 'log' (always on, the floor), 'desktop', 'slack', 'discord', 'discord_follow'. Slack/Discord webhook URLs live in the OS keyring (cherrypick secrets-set --channel slack|discord|discord_follow), never in this file or env vars. 'discord_follow' is a SECOND Discord webhook with its own keyring entry so the Follow Feed can post to its own channel instead of sharing the trade channel.",
    "channels": [
      "log",
      "desktop"
    ],
    "trade_channels": [
      "log",
      "discord"
    ],
    "desktop_app_name": "cherrypick",
    "trade_summary": {
      "_comment": "MEIC risk_profile prefixes routed to a periodic per-symbol digest instead of a per-trade push (an uncapped sampling stream's 100+ trades/day would otherwise spam every entry/exit individually; built for the wing-width study, retired 2026-08-05, kept for every arm family since). Rows whose risk_profile starts with one of these prefixes are excluded from the normal per-trade lines. Updated 2026-08-07 for the arms cutover: `open`/`width-5`/`width-10` are the uncapped-sampling streams this exists for; `control` runs at book-like volume (overlap_scope 'shorts', capped concurrency) but is included for consistency, same as the old ladder names were. The retired ladder/GEX prefixes (conservative/moderate/aggressive/very-aggressive/gex-) are kept too — they still match any historical row from before the cutover.",
      "profile_prefixes": [
        "control",
        "open",
        "width-",
        "gex-",
        "conservative",
        "moderate",
        "aggressive",
        "very-aggressive"
      ],
      "interval_minutes": 15
    }
  },
  "eod_header": "x",
  "eod_digest": {
    "_comment": "Suite end-of-day digest: writes logs/eod-digest-<day>.md (one session's cross-module paper P&L, net of costs, plus a pointer to each module's own paper-eod file) and pushes a one-line summary through your notify channels. EVENT-DRIVEN (no fixed clock time): the watchdog fires it once EVERY installed module has written its paper-eod-<day>.md, so it can't race a module that settles a few minutes late (the old fixed 16:15 missed flies when it settled at 16:20). 'deadline' (ET, box-local clock) is the backstop - if some module is late or never writes (a flat flies session writes none), the digest fires anyway at the deadline with whatever exists, so it never skips. ON by default; OPT OUT with enabled=false then re-run `install` (removes the trigger's effect by disabling) or `uninstall`. 'task_name' is kept only so install/uninstall can remove a stale fixed-time task from a prior install. Read-only over the paper DBs; fired detached, off the watchdog reliability path.",
    "enabled": true,
    "task_name": "cherrypick-eod-digest",
    "deadline": "16:45"
  },
  "eod_insight": {
    "_comment": "AI EOD insight (`cherrypick eod-insight`). When enabled AND Claude Code (`claude`) is on PATH, pipes the day's DETERMINISTIC reports (each module's eod-analysis + paper-eod, and the suite digest) to Claude in headless print mode and writes a synthesized cross-module narrative to logs/eod-insight-<day>.md (linked from the dashboard EOD card). EVENT-DRIVEN: the watchdog launches it DETACHED on the same completion event as the digest (all modules' paper-eod present, or the digest deadline) - no fixed clock time - so the AI call never runs in the watchdog process. Claude runs with NO execution/edit/arbitrary-URL tools (Bash/Edit/Write/WebFetch/Task always disallowed) and never touches the broker or config - the orchestrator writes the file. 'research_events' (default true) additionally grants WebSearch ONLY, so the insight can research upcoming macro events (FOMC/CPI/jobs) and highly anticipated earnings for a forward-looking section (anchored on the suite's deterministic FOMC/expiry/holiday calendar); set false for a purely offline, reports-only synthesis. OFF by default overall (needs Claude + an authenticated session + a paid API call, and research adds web calls + latency); the deterministic eod-analysis remains the source of record and researched notes are best-effort. Off the watchdog reliability path; best-effort. Turn on with enabled=true then re-run `install`. 'model' null uses Claude's configured default. Paper/educational only, not financial advice.",
    "enabled": false,
    "task_name": "cherrypick-eod-insight",
    "model": null,
    "timeout_seconds": 120,
    "research_events": true
  },
  "data_header": "x",
  "data_epoch": {
    "_comment": "Optional restatement marker. Declare when a correctness fix RESTATES what recorded paper history means (e.g. the phase-0 leg-ratio / fee-count / win-definition fixes): set 'date' to the first session traded on fixed code. `report` stays descriptive (totals still cover all history, plus a pre_epoch_trades count per module); `calibrate` ENFORCES it - promotion readings use only sessions on/after the date, so a rung can never graduate on numbers produced by retired code. Remove or leave date null when no restatement is in effect.",
    "date": null,
    "note": null
  },
  "log_archive": {
    "_comment": "End-of-month log/report rotation (`cherrypick archive`). A monthly scheduled task zips each FINISHED month's dated reports (paper-eod / eod-analysis / eod-digest / live eod) and rotated log backups (*.log.N) into logs/archive/<YYYY-MM>/<scope>.zip - one zip per scope (the suite logs root + each module dir) - then removes the originals once the zip verifies. Idempotent and safe: it never touches the current month or an active .log, and re-runs converge. ON by default and registered by `install`; OPT OUT with enabled=false then re-run `install`/`uninstall`. 'day' is the day of month it fires (1..28), 'at' the box's local clock. Files only; off the watchdog reliability path. Run on demand any time with `cherrypick archive` (add --dry-run to preview, --month YYYY-MM to scope to one month).",
    "enabled": true,
    "task_name": "cherrypick-log-archive",
    "day": 1,
    "at": "03:30"
  },
  "advise_header": "x",
  "advise": {
    "_comment": "Per-session parameter advice (`cherrypick advise`) - Tier 1 of the agentic layer: agent advises, loop decides. Runs Claude headless over the day's DETERMINISTIC artifacts only (module EOD reports, suite digest, report/calibrate JSON, the shared market calendar; ALL tools denied, unlike eod_insight's research mode) and asks for parameter proposals for the NEXT session, restricted to each module's 'advice_bounds' manifest of closed legal ranges. The orchestrator validates with cherrypick.core.advice and writes state/advice/<module>-<session>.json; the module's paper loop re-validates with the SAME core code at session start and treats absent/stale/invalid advice as baseline - one bad proposal rejects the whole set (the rejections are still written, for audit). OFF by default TWICE: 'enabled' here AND per-module 'modules.<name>.enabled' must both be true. Kill switch: flip either flag, or delete the advice file. Paper loops only; advice can never widen behavior beyond the declared bounds.",
    "enabled": false,
    "model": null,
    "timeout_seconds": 180,
    "modules": {
      "meic": {
        "enabled": false,
        "advice_bounds": {
          "stop_trigger_ratio": {
            "min": 0.85,
            "max": 0.95
          },
          "daily_ic_trade_target": {
            "min": 0,
            "max": 3
          },
          "entry_price_strategy": {
            "choices": [
              "mid",
              "natural_bid",
              "ioc_step",
              "day_improve",
              "auto"
            ]
          }
        }
      }
    }
  },
  "reconcile": {
    "_comment": "`cherrypick reconcile` (and the serve-only /api/reconcile dashboard card): a paper<->live isolation guard. It enumerates EVERY account on the login (list_accounts - tastytrade returns multiple per user) and checks each (read-only get_positions/get_account_info), flagging any open positions/BP a paper-only suite shouldn't have. On-demand and off the watchdog reliability path; never trades; account numbers masked. Optional 'broker_module' forces which module's tt.py to query; omit to auto-discover the first positions-capable one.",
    "broker_module": null,
    "schedule": {
      "_comment": "Phase-5 posture: once any module trades live, reconcile stops being on-demand-only and runs daily as its own scheduled task ('at' in market-timezone HH:MM, after the close). The task runs `reconcile --scheduled`, which pushes a notification on any non-FLAT verdict (DRIFT -> CRITICAL, UNKNOWN -> WARNING) and stays quiet when flat. Its own task, deliberately OFF the watchdog tick - the broker call never rides the reliability path. Off by default; enable then re-run `install`.",
      "enabled": false,
      "task_name": "cherrypick-reconcile",
      "at": "16:30"
    }
  },
  "symbol_watch": {
    "_comment": "Earnings forward-preview scan (`python -m cherrypick.earnings.symbol_watch refresh`) - the source of scout's read-only Earnings page 'Upcoming' section (expected move, term structure, IV/RV ratio, winrate, historical move stats, and a recommended/near-miss/fail tier badge for symbols reporting in the next 'days' TRADING days, restricted to a liquid-enough universe - tastytrade's 'Liquid Symbols' + 'High Options Volume' + 'tasty Earnings' watchlists). Purely informational: never touches a paper/live ledger, never places an order. Writes a JSON snapshot into the earnings module's own data directory; scout reads it read-only. Its own daily task, deliberately OFF the watchdog tick - a multi-minute per-symbol broker-chain scan has no place on the reliability path. Only meaningful once the 'earnings' module itself is installed and enabled (this task shells into that module's own code). OFF by default; enable then re-run `install`.",
    "enabled": false,
    "task_name": "cherrypick-earnings-symbol-watch",
    "at": "06:30",
    "days": 10
  }
}
