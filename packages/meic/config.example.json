{
  "instrument_and_sizing_header": "===== Instrument & Sizing =====",
  "symbols": ["SPX"],
  "_symbols_note": "List of underlyings to trade concurrently, e.g. [\"SPX\", \"XSP\"]. Each symbol gets its own live ATM subscription window and its own GEX channel (net_gex/gamma_flip/call_wall/put_wall computed from that symbol's own OI+greeks) — there is no longer a separate gex_symbol; GEX is 1:1 with every traded symbol. The single-symbol 'symbol' key is still accepted as a deprecated alias for [\"symbol\"] if 'symbols' is absent, but new configs should use 'symbols'. max_concurrent_ics and max_entries_per_day are account-wide totals shared across every symbol in this list, not per-symbol caps. Correlated symbols (e.g. SPX and XSP) are not currently exposure-limited against each other — avoid combinations that double up on the same directional risk until that guard exists.",
  "delta_target": 0.18,
  "_delta_target_note": "Superseded by the VIX-banded scale below when the market-wide VIX (fetched once per iteration in Step 4a) falls in a defined band -- delta_target itself is the fallback only if VIX is unavailable. See delta_target_vix_low/elevated/high/crisis and vix_band_low_max/elevated_max/high_max.",
  "delta_target_vix_low": 0.16,
  "delta_target_vix_elevated": 0.14,
  "delta_target_vix_high": 0.12,
  "delta_target_vix_crisis": 0.10,
  "vix_band_low_max": 18,
  "vix_band_elevated_max": 25,
  "vix_band_high_max": 35,
  "_delta_target_vix_bands_note": "VIX <= vix_band_low_max (18): delta_target_vix_low (0.16). VIX <= vix_band_elevated_max (25): delta_target_vix_elevated (0.14). VIX <= vix_band_high_max (35): delta_target_vix_high (0.12). VIX > vix_band_high_max: delta_target_vix_crisis (0.10) -- note VIX > regime_vix_pause_threshold (25) already pauses new IC entries account-wide, so the high/crisis bands mostly affect what a scan *would* have targeted, not what actually gets entered; kept for consistency and for ORB debit-spread strike selection, which isn't blocked by the VIX regime gate. Documented trader convention (VIX-regime delta scaling), not a backtested-optimal breakpoint set for this specific strategy -- treat as a reasoned starting point, not a proven-optimal one.",
  "max_wing_width": 10,
  "_max_wing_width_note": "Upper bound (points) on spread width; the agent chooses the actual wing width per entry (any reasonable value up to this max) based on credit, buying power, and session conditions rather than scanning a fixed list.",
  "wing_widths_by_symbol": {
    "SPX": [5, 10],
    "XSP": [1, 2, 5],
    "QQQ": [5, 10],
    "IWM": [2, 5],
    "DEFAULT": [2, 5, 10]
  },
  "_wing_widths_by_symbol_note": "Per-symbol IC wing-width candidate shortlist (points), scanned widest-first. Wing width is a dollar-denominated risk parameter, so it must be set per instrument rather than shared across price levels: 10 points is ~0.13% of SPX (~7500) but ~3.4% of IWM (~297). Values respect each symbol's strike increment and stay within max_wing_width. DEFAULT covers any symbol not listed.",
  "quantity": 1,
  "cash_settled_symbols": ["SPX", "XSP", "NDX", "RUT"],
  "_cash_settled_symbols_note": "Symbols that settle in cash at expiration (SPX/XSP/NDX/RUT). Membership drives the end-of-day exit: cash-settled positions are LEFT TO EXPIRE (settled at expiration_settlement_time) rather than force-closed, since there is no assignment risk; symbols NOT listed (QQQ/IWM/equities/futures) are physically settled and force-closed before the bell to avoid assignment. It also controls whether a missed non-cash force-close is escalated as a critical assignment-risk failure. Leave any equity or physically-settled symbol you add to 'symbols' out of this list.",

  "trading_window_and_caps_header": "===== Trading Window & Daily Caps =====",
  "enable_live_trading": false,
  "_enable_live_trading_note": "Set to true to allow execute_trade/adjust_order/close_position to submit real orders. Requires tastytrade OAuth credentials in the OS keyring (via `python src/tt.py secrets_set`). Left false in this example so a fresh setup starts in dry-run.",
  "account_deploy_limit_pct": 0,
  "_account_deploy_limit_pct_note": "Account-level deploy cap (via cherrypit.risk). 0 = OFF (default). A positive value (e.g. 50) blocks a LIVE execute_trade whose buying power would push total deployed BP above that percent of account capacity (used + available derivative buying power). Fail-closed: if account state can't be read, the live order is blocked. Enforced only on live submits; a dry-run reports the governor verdict without blocking. Does not affect paper trading.",
  "loop_interval_minutes": 5,
  "entry_window_start": "10:00",
  "entry_window_end": "14:30",
  "force_close_time": "15:45",
  "_force_close_time_note": "Backstop force-close time (ET) for NON-cash-settled symbols (they primarily close at the earlier physical_settlement_force_close_time). Cash-settled symbols are NOT force-closed here — they are left to expire (see expiration_settlement_time). Event force-closes (FOMC 13:30, triple-witching/quarterly 14:00) still apply to all symbols.",
  "daily_ic_trade_target": 2,
  "_daily_ic_trade_target_note": "Target number of IC entries per day. This is a guidance heuristic, not a hard cap — buying power, max_concurrent_ics, and regime gates remain the binding constraints. The agent uses this target to calibrate position sizing and entry selectivity (e.g., if target is 2 and 1 is already open, remaining slots are reserved for only high-conviction setups). Set to 0 to disable IC entries and run ORB only.",
  "max_concurrent_ics": 4,
  "_risk_profiles_note": "Rather than hand-editing config keys, use /set-risk-profile to switch between named presets: conservative (default, safest), moderate (recommended after 2–4 weeks), aggressive (after 2+ weeks at moderate with 60%+ win rate), very-aggressive (deliberate short experiments only). Each profile bundles related gate thresholds with offsetting position-cap and stop-management constraints. See config.risk.json and docs/risk-profiles.md for full rationale.",

  "entry_price_strategy_header": "===== Entry Price Strategy =====",
  "entry_price_strategy": "auto",
  "_entry_price_strategy_note": "auto (default): agent chooses per-iteration based on session, spread width, and IV rank rather than always trying mid first and eating the wait. mid: Day limit at streaming mid price; falls back to natural_bid if avg per-leg spread > mid_spread_gate or session is open_volatile/late, or if unfilled after mid_improve_wait_seconds. natural_bid: immediate fill price. ioc_step: IOC attempts above natural bid stepping down. day_improve: Day limit at natural_bid + day_improve_amount.",
  "max_credit": null,
  "separate_spread_entry": false,
  "_separate_spread_entry_note": "Controls IC order structure. false = always 4-leg combo. true = always separate 2-leg spreads. \"auto\" = agent evaluates per-iteration: favors separate when IV rank > 0.35, session is late/open_volatile, or 2+ ICs already open; falls back to separate if combo dry_run returns warnings.",
  "mid_improve_wait_seconds": 45,
  "mid_spread_gate": 0.10,
  "ioc_step_increments": [0.02, 0.01],
  "ioc_step_wait_seconds": 10,
  "day_improve_amount": 0.03,
  "day_improve_wait_seconds": 60,
  "pre_submit_requote_threshold": 0.03,

  "credit_and_fee_floors_header": "===== Credit & Fee Floors =====",
  "min_credit_pct_of_width": 0.15,
  "_min_credit_pct_of_width_note": "Minimum credit as fraction of wing width; reject entries below this (e.g. 2-wide must collect >= $0.30).",
  "low_iv_credit_floor_iv_rank_max": 0.35,
  "low_iv_min_credit_pct_of_width": 0.10,
  "_low_iv_note": "When IV rank is >= min_iv_rank but <= low_iv_credit_floor_iv_rank_max, the credit floor relaxes to low_iv_min_credit_pct_of_width instead of min_credit_pct_of_width.",
  "_min_net_credit_after_fees_note": "The fee-adjusted floor no longer uses a flat dollar constant (retired 2026-07-06). It now requires (ic_natural_bid * dollar_multiplier) - est_fee_per_contract to clear the same applicable pct-of-width floor above, converted to dollars. The old standalone min_credit absolute-dollar-per-symbol floor was also retired as redundant once this fee check became width/IV-aware.",
  "fee_estimate_lookback_trades": 20,
  "fee_estimate_min_sample_size": 5,
  "_fee_estimate_fallback_note": "The per-symbol fee fallback (used until fee_estimate_min_sample_size closed trades exist) is now COMPUTED by `db.py get_fee_estimate` (field fallback_per_contract, via cherrypit.fees.ic_open_fee) from the shared tastytrade schedule, not a config list.",

  "stops_header": "===== Stops (no profit target) =====",
  "stop_type": "spread",
  "_stop_type_note": "spread (default, and currently the only supported mode): software stop monitors the IC's cost and closes the spread when the trigger ratio is hit. tastytrade does not support exchange-level multi-leg stop orders for combos.",
  "stop_trigger_ratio": 0.95,
  "_stop_trigger_ratio_note": "Fraction of net_credit at which a per-side stop triggers. Default 0.95 = stop fires when that side's cost reaches 95% of the total IC credit.",
  "stop_limit_ratio": 1.02,
  "_stop_limit_ratio_note": "Cushion multiplier on the marketable closing debit ((short_ask - long_bid) * stop_limit_ratio) so the Day limit stays marketable even if the quote ticks against you before the order reaches the exchange.",
  "per_side_stop_management": true,
  "per_side_stop_trigger": "full_credit",
  "max_stop_adjustments_per_ic": 3,
  "_no_profit_target_note": "MEIC has NO profit target. An iron condor is only ever closed by a per-side stop, a time-based force-close (non-cash-settled symbols), or an event force-close; cash-settled symbols are left to expire and settled at the close. Do not add a profit_target_pct key. (ORB, a separate directional debit spread, has its own orb_profit_target_pct.)",

  "settlement_header": "===== Settlement & Physical Assignment =====",
  "expiration_settlement_time": "16:00",
  "_expiration_settlement_time_note": "0DTE PM-settlement time (ET). At/after this, cash-settled positions still open (not stopped) are LEFT TO EXPIRE and settled: OTM shorts keep the full credit; an ITM short settles for its intrinsic value capped at the wing width. Non-cash-settled symbols are force-closed before this (see physical_settlement_force_close_time) to avoid assignment.",
  "physical_settlement_force_close_time": "15:30",
  "_physical_settlement_force_close_time_note": "Force-close time (ET) for symbols NOT in cash_settled_symbols. Physically-settled American-style options (QQQ/IWM/etc.) are flattened before the bell to avoid assignment/pin risk (15:45 force_close_time is a backstop). Cash-settled symbols are NOT force-closed at EOD — they are left to expire (see expiration_settlement_time).",
  "physical_settlement_exit_friction": 0.05,
  "_physical_settlement_exit_friction_note": "PAPER-TRADING ONLY. Per-spread price friction added to the cost-to-close when force-closing a physically-settled position, so paper P&L reflects the wider spreads / assignment risk real QQQ/IWM closes face vs a clean cash settlement. Live trading pays real fills, so this does not apply there.",
  "pin_risk_threshold_pct": 0.002,
  "_pin_risk_threshold_pct_note": "PAPER-TRADING ONLY. A short strike within this fraction (0.2%) of the underlying at force-close is treated as pinned (ambiguous assigned-vs-worthless outcome), triggering pin_risk_penalty_pct_of_width.",
  "pin_risk_penalty_pct_of_width": 0.25,
  "_pin_risk_penalty_pct_of_width_note": "PAPER-TRADING ONLY. Extra force-close cost, as a fraction of wing width, added on a pinned physically-settled short.",

  "delta_and_otm_guardrails_header": "===== Delta & OTM Guardrails =====",
  "max_call_delta_entry": 0.20,
  "max_call_delta_entry_open_volatile": 0.19,
  "max_call_delta_entry_late": 0.19,
  "min_call_otm_pct": 0.0035,
  "min_put_otm_pct": 0.003,
  "_otm_pct_note": "OTM distance as a fraction of underlying price, e.g. (strike - underlying_price) / underlying_price for calls. Percentage-based so no rescaling is needed if symbol changes.",

  "iv_and_regime_gates_header": "===== IV & Regime Gates =====",
  "min_iv_rank": 0.30,
  "regime_vix_pause_threshold": 25,
  "regime_vix1d_ratio_pause_threshold": 1.30,
  "_regime_vix1d_ratio_note": "vix1d / vix ratio, computed once per iteration in Step 4a (market-wide, like VIX -- not symbol-specific). Documented trader convention: ratio >= 1.0 is an 'expansion day' (above-average move priced in), >= 1.30 is an 'event day' (Fed/CPI/shock priced in -- not the environment for short premium without substantial width and reduced size), <= 0.75 is a 'compression day'. 1.30 is used here as the IC-entry pause threshold, same role as regime_vix_pause_threshold but a same-day-specific signal rather than a 30-day-window one. Not independently backtested for this strategy -- a reasoned starting point from documented convention, not a proven-optimal breakpoint.",
  "regime_atr_lookback_days": 5,
  "regime_atr_pause_threshold_pct": 0.015,
  "_regime_atr_pause_threshold_pct_note": "Pause IC entries when a symbol's 5-day ATR exceeds this fraction of its underlying price (0.015 = 1.5%). Percentage-based so it means the same 'elevated realized vol' across every symbol — a fixed points threshold silently broke when symbols span 297 (IWM) to 7500 (SPX). ORB entries remain eligible when this pauses ICs.",
  "late_entry_bias_enabled": true,
  "late_entry_bias_iv_rank_max": 0.45,
  "late_entry_bias_start_time": "12:00",

  "orb_header": "===== Opening Range Breakout (ORB) =====",
  "orb_enabled": true,
  "orb_range_minutes": 5,
  "orb_breakout_threshold_pct": 0.005,
  "orb_wing_width_by_symbol": {
    "SPX": 20,
    "XSP": 2,
    "QQQ": 10,
    "IWM": 5,
    "DEFAULT": 20
  },
  "_orb_wing_width_by_symbol_note": "Per-symbol ORB debit-spread wing width (points). Like wing_widths_by_symbol, must scale per instrument — 20 points is 0.27% of SPX but 6.7% of IWM. DEFAULT covers any symbol not listed.",
  "orb_entry_window_end": "12:00",
  "orb_profit_target_pct": 1.00,
  "orb_stop_loss_pct": 0.50,
  "orb_close_time": "15:30",

  "quarterly_expiry_and_triple_witching_header": "===== Quarterly Expiry & Triple Witching =====",
  "quarterly_expiry_skip_open_volatile": true,
  "quarterly_expiry_min_call_otm_pct": 0.0067,
  "quarterly_expiry_max_intraday_range_pct": 0.005,
  "_quarterly_expiry_max_intraday_range_pct_note": "Halt new entries on quarterly-expiry dates once the underlying's intraday range (high-low) exceeds this fraction of its price (0.005 = 0.5%). Percentage-based so it scales across symbols; replaces the old fixed 35-point value that only made sense for SPX.",

  "fomc_header": "===== FOMC Calendar & Blackout =====",
  "fomc_blackout_start": "13:30",
  "fomc_blackout_end": "14:30"
}
