export function PlaceholderPage({ title }: { title: string }) {
  return (
    <div className="page">
      <h1>{title}</h1>
      <p className="muted">Coming in a later milestone.</p>
    </div>
  );
}
