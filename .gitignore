# cherrypick — local/runtime artifacts
__pycache__/
*.pyc
.tmp/
.pytest_cache/
.ruff_cache/

# Packaging build artifacts (regenerated by pip/build; not source)
build/
*.egg-info/

# Runtime state & logs (machine-local)
logs/
state/

# Generated status dashboard (regenerated on each watchdog tick; not source)
dashboard.html

# Working config (copy from config.example.json; may hold machine-specific paths)
config.json

# Opt-in local tooling (never committed; see plan Concept H)
graphify-out/
.claude/
